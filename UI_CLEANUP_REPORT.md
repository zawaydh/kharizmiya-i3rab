# تقرير تنظيف نظام الواجهة

## الهدف

إعادة واجهات الأنشطة إلى نموذج بصري واحد مستوحى من لوحة العمل النظيفة في الصورة المرجعية: خلفية داكنة هادئة، ولوحة بيضاء رئيسية، وخيارات مرتبة بلا بطاقات متراكبة.

## ما تغير

### 1. إزالة طبقات الإصلاح المتنافسة

حُذفت ملفات الواجهة النهائية المتعارضة التالية من المشروع:

- `80-typography-responsive.css`
- `90-formal-theme.css`
- `95-classic-readable.css`
- `96-contrast-menu-path-fixes.css`
- `97-unified-workspaces.css`
- `98-stable-activity-system.css`

واستُبدلت بملف نهائي واحد:

- `app/styles/80-clean-system.css`

أصبح `app/globals.css` يستورد تسعة ملفات مرتبة فقط، وتنتهي السلسلة بنظام الواجهة النظيف الواحد.

### 2. تنظيف شريط القائمة

- اعتماد `position: sticky` فقط وإلغاء نموذج القائمة الثابتة المتضارب.
- إزالة أي حجز أو مسافة أعلى الصفحة.
- تثبيت ارتفاع القائمة: 64px على الحاسوب و58px على الشاشات الأصغر.
- توحيد أحجام الشعار واسم المنصة والروابط.
- التحول إلى قائمة الهاتف عند عرض 960px لمنع تزاحم روابط العربية.
- إعادة بناء قائمة الهاتف كدرج واحد واضح، مع شاشة مستقلة للموضوعات بدل تراكب القوائم.

### 3. لوحة عمل واحدة

- توحيد عرض الأنشطة الأساسية عند 920px والمسارات عند 1080px.
- اعتماد حد أدنى ثابت بصريًا لمساحة العمل، مع السماح لها بالتمدد عند طول الشرح.
- إزالة `max-height` و`overflow:hidden` من الإطار الرئيس لمنع قص المحتوى.
- إبقاء التمرير الداخلي فقط في مواضع تحتاجه فعلًا، مثل خريطة المسار والتلميح الطويل.

### 4. إزالة البطاقات المتراكبة

- جعل بطاقة النشاط الخارجية هي الإطار المرئي الوحيد.
- تحويل بطاقة الجملة والسؤال والمناطق الداخلية إلى أقسام مسطحة يفصل بينها خط خفيف.
- إخفاء سجل البطاقات المحلولة المتراكم أعلى السؤال؛ شريط التقدم يكفي لبيان موقع الطالب.
- جعل نتيجة المسار البصري جزءًا من التدفق الطبيعي بدل وضعها فوق الخريطة بـ`position:absolute`.
- دمج جملة الاختبار، والسؤال، والخيارات، وأزرار التنقل في لوحة واحدة بدل بطاقتين متتاليتين.

### 5. توحيد الخطوط والأحجام

اعتمد سلم طباعي محدود:

- النصوص الثانوية: 12–14px.
- النص العادي والخيارات: 15–16px.
- السؤال: 18–22px.
- الجملة: 24–33px بحسب عرض الشاشة.
- عناوين الصفحات: 24–30px.

لم يعد اسم المنصة أو وصف القائمة ينخفض إلى 9–10px، ولم تعد الجملة تقفز إلى أكثر من 40px.

### 6. تحسين بنية HTML

- أصبح `<main>` الرئيس موجودًا في `app/layout.js` فقط.
- استُبدلت عناصر `<main>` المتداخلة في صفحة الموضوعات ولعبتي النصوص والكلام بعناصر `<div>`.
- أضيفت الفئة `platform-main` إلى الغلاف الرئيس لتطبيق نظام المسافات دون التأثير في بقية الحاويات.

## الملفات المعدلة الأساسية

- `app/globals.css`
- `app/styles/80-clean-system.css`
- `app/styles/README.md`
- `app/layout.js`
- `app/components/exercise/QuizExperienceViews.tsx`
- `app/components/DiacriticsTextGame.tsx`
- `app/components/I3rabInOurSpeechGame.tsx`
- `app/topics/page.js`
- `tests/cssArchitecture.test.ts`

## نطاق التعديل

لم تُعدّل أشجار الإعراب، أو الأمثلة، أو أسئلة الاختبار، أو منطق حفظ التقدم، أو إعدادات Supabase. التعديل مخصص لنظام العرض وبنية واجهة الاختبار.

## الفحوص المنفذة

- تحليل `80-clean-system.css` بواسطة محلل CSS: لا توجد أخطاء قواعد أو تصريحات.
- التحقق من تطابق الأقواس والتعليقات في ملف CSS.
- التحقق من وجود جميع ملفات CSS المستوردة وترتيبها.
- فحص TypeScript وصل إلى مرحلة تحليل الملفات دون أخطاء صياغة في الملفات المعدلة؛ تعذر الفحص الكامل لأن تثبيت حزم npm لم يكتمل في بيئة الفحص.
